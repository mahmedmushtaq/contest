package com.contest.competition.classes.interfaces.requestinterfaces;

import com.contest.competition.classes.models.NotificationData;

import java.util.ArrayList;

public interface OnLoadingNotification {
    void onLoading(ArrayList<NotificationData> notificationData);
}
